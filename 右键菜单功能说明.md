# 桌面速记 — 右键菜单功能说明

> 适用范围：在**右侧笔记列表**中，用鼠标右键点击任意一条笔记卡片时弹出的菜单。
> 更新日期：2026 年 8 月 16 日

---

## 一、菜单入口

- 鼠标悬停在右侧「我的提示词」列表中的任意笔记卡片上，点击**鼠标右键**即可弹出。
- 菜单采用**白色圆角面板**设计（圆角 10px、浅灰细边框），四周无系统阴影、无黑边。

---

## 二、菜单功能列表

| 顺序 | 功能项 | 图标 | 点击后行为 |
| ---- | ------ | ---- | ---------- |
| 1 | **删除提示词** | 红色垃圾桶 | 弹出确认框「确定要删除「笔记名」吗？」，点击"是"后删除该笔记；若列表仍有其他笔记，自动切换到第一条 |
| 2 | **重命名** | 灰色铅笔 | 弹出输入框，预填当前笔记名称；输入新名称点击确定后保存（空名称会被拒绝） |
| — | 分隔线 | — | 将"删除/重命名"与"更改颜色"分区 |
| 3 | **更改提示词颜色**（分区标题，不可点击） | 灰色调色板 | 仅作说明文字，下方列出 3 种可选颜色 |
| 3.1 | 黑色（默认） | 黑色圆点 | 笔记卡片右侧渐变为黑色调（透明度 15%） |
| 3.2 | 红色 | 红色圆点 | 笔记卡片右侧渐变为红色调（透明度 38%） |
| 3.3 | 蓝色 | 蓝色圆点 | 笔记卡片右侧渐变为蓝色调（透明度 38%） |

---

## 三、功能细节说明

### 1. 删除提示词
- 触发后弹出系统确认对话框，**默认选中"否"**，避免误删。
- 删除后数据立即落盘，列表自动刷新。
- 若删除的是当前打开的笔记，编辑器会自动加载列表中的第一条笔记；列表为空则编辑器清空。

### 2. 重命名
- 输入框预填当前名称，方便直接修改。
- 确认后名称**自动去除首尾空格**；纯空名称不生效。
- 重命名后顶部标题栏、列表卡片同步更新。

### 3. 更改提示词颜色
- 当前正在使用的颜色前会显示**白色对勾**（画在色点图标内）。
- 颜色只作用于笔记卡片的**右侧渐隐渐变**（基色 + 透明度），卡片边框、标题、日期始终为默认黑色样式，不随颜色变化。
- 切换颜色后立即落盘，下次启动保持。

---

## 四、菜单交互体验

| 项目 | 说明 |
| ---- | ---- |
| 弹出动画 | 打开时**淡入 + 从下方 10px 上浮**，时长 190ms，OutCubic 缓动 |
| 悬停高亮 | 菜单项悬停时浮出浅灰圆角高亮（#EEF2F8） |
| 外观 | 纯白背景、圆角 10px、1px 浅灰边框（#E2E6EE）、无系统阴影 |
| 快捷键 | 支持键盘上下键选择、回车确认、Esc 关闭 |
